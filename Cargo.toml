[package]
name = "elm_kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
linked_list_allocator = "0.10.5"
bootloader = "0.9.23" # Adds the bare-metal boot sequence

[profile.release]
opt-level = 3
panic = "abort"
lto = true
